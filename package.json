{
  "scripts": {
    "deploy": "gcloud functions deploy history --runtime nodejs8 --trigger-http --memory=256MB",
    "deploy_cron": "gcloud app deploy cron.yaml",
    "deploy_app": "gcloud app deploy app.yaml"
  },
  "devDependencies": {
    "eslint": "^5.8.0"
  },
  "dependencies": {
    "@google-cloud/bigquery": "^1.3.0",
    "axios": "^0.18.1",
    "bluebird": "^3.5.2",
    "implied-volatility": "^1.0.0",
    "moment": "^2.22.2"
  }
}
